# HTTP Server
server:
  port: ${PORT:8093}

# Spring properties
spring:
  application:
    name: product-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/