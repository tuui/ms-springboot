# HTTP Server
server:
  port: ${PORT:8092}

# Spring properties
spring:
  application:
    name: order-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/